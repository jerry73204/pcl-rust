//! Core traits for the new marker type system
//!
//! This module now only contains utility functions that work with concrete point types.
//! The old trait-based system has been completely removed in favor of marker types.

// All deprecated traits have been removed - use the new marker type system instead

#[cfg(test)]
mod tests {
    // Tests removed - old trait system no longer exists
}
